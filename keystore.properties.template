# Copy this file to `keystore.properties` (git-ignored) and fill in your values.
# Create a keystore once with:
#   keytool -genkey -v -keystore foco-release.jks -keyalg RSA -keysize 2048 -validity 10000 -alias foco
# Then point storeFile at it (relative to the project root).
storeFile=foco-release.jks
storePassword=CHANGE_ME
keyAlias=foco
keyPassword=CHANGE_ME
